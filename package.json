{
  "name": "ipfs-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "((cd proxy && cp .env.sample .env && npm install) & (cd api && cp .env.sample .env && npm install) & (cd client && npm install))",
    "build": "cd client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pm2": "^4.5.6"
  }
}
